# to perform flex-box operation we need a parent container.
# to redesign and restyle and to give a postion (row,column) we use flex box
# line-height:- 

# default flex box - row
# every block level elements start in a new line
# when we use display flex in the  parent class all the child elements inside the parent class convert from x axis 

# flex-direction